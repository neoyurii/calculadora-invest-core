package com.core.Presenters;

import java.util.HashMap;

import com.core.Kernel.Presenter.Presenter;
import com.core.Kernel.Presenter.PresenterEnum;
import com.core.Kernel.Presenter.PresenterFactory;

public class CalculatorPresenterFactory extends PresenterFactory {
    private final HashMap<PresenterEnum, Presenter<?>> presenters = new HashMap<PresenterEnum, >()
}
